# Python 版本要求：3.10 ~ 3.12（见 scripts/setup.ps1）
#   - 3.9 及以下：pillow 12 起要求 >=3.10，装不上新版本
#   - 3.13 及以上：rapidocr-onnxruntime 要求 <3.13，会直接安装失败
# 用 >= 约束即可：pip 会在当前解释器上自动挑出兼容的最高版本。

uiautomator2>=3.0.0
rapidocr-onnxruntime>=1.3.0
pillow>=10.0.0
