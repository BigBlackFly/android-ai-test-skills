# 链路：联想日历_168_图库导入入口

- 来源会话: #165（PASS，2026-09-22T15:45:17.022）
- 包名: com.zui.calendar
- 设备: HA2J36T6
- 用例口述:

```
联想日历_168

前提：确保设备没有课程表。如果存在需要删除所有课程表。

操作步骤：
1. 日历点击页面右上角更多按钮，选择课程表
2. 点击"从图库导入课程表"按钮
3. 验证可进入图片选择入口

预期结果：
1. 可打开课程表页面
2. 图库导入入口可点击
3. 可正常进入图片选择入口

```

## 步骤

   - ⚙️ pm clear com.zui.calendar
1. **act tap** 定位 `rid=button1(同意)` —— 首次启动：App权限说明框同意（后续权限框自动点）
   - 🔐 权限弹窗自动响应：允许, 允许, 允许, 允许, 允许
   - 👁 检查点：activity=.timetable.widget.TimetableGuideActivity
2. **act tap** 定位 `rid=timetable_guide_button(我知道了)` —— 关课程表上新引导
   - 👁 检查点：activity=.AllInOneActivity
3. **act tap** 定位 `rid=iv_more` —— 步骤1: 点右上角更多按钮
   - 👁 activity=.AllInOneActivity（与前一步相同，连续重复核对）
4. **act tap** 定位 `rid=item_title(课程表)` —— 步骤1: 选择课程表
   - 👁 检查点：activity=.timetable.display.TimetableActivity
5. **act tap** 定位 `rid=btnImportFromGallery` —— 步骤2: 点从图库导入课程表按钮
   - 🪟 弹窗：「请确保图片清晰、完整」
   - 👁 activity=.timetable.display.TimetableActivity（与前一步相同，连续重复核对）
6. **act tap** 定位 `rid=button1(知道了)` —— 关掉图片提示框，触发图库权限/选择器
   - 🔐 权限弹窗自动响应：全部允许
   - 👁 检查点：activity=com.android.providers.media.photopicker.PhotoPickerGetContentActivity

## 结论

联想日历_168 复跑通过（3/3）。前提:pm clear 后无课程表、权限未授予。预期1:首页→更多→课程表，落到 TimetableActivity 空状态页;预期2:空状态页三入口可见,点「从图库导入课程表」弹「请确保图片清晰、完整」→点知道了;预期3:系统照片权限框自动点允许→进入 PhotoPicker（PhotoPickerGetContentActivity,含照片/相册页签）,READ_MEDIA_IMAGES granted=true。全程只用 act --full，无额外 observe。证据 storage/evidence/c168/

## 回放说明

- 按步骤逐条执行，**每步核对返回**；任一步失配（元素找不到/状态不符）
  → 回退完整 observe→决策流程，跑通后重新 export 覆盖本卡。
- 带 🎯 的步骤会声明权限意图，照做即可；双分支场景注意两轮之间的 revoke。
- 带 🪟 的步骤说明当时是弹窗操作，`rid=button1` 之类**不能盲目复用**，
  要按弹窗标题确认当前是哪一层弹窗。
- 坐标为当次实测，⚠️ 标记的步骤回放前必须先 observe 校准。

> 提炼自会话 #165；回放失配 → 回退全流程并更新本卡。