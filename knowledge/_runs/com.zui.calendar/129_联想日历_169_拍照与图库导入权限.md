# 链路：联想日历_169_拍照与图库导入权限

- 来源会话: #129（PASS，2026-09-22T14:43:53.135）
- 包名: com.zui.calendar
- 设备: HA2J36T6
- 用例口述:

```
联想日历_168

前提：确保设备没有课程表。如果存在需要删除所有课程表。

操作步骤：
1. 日历点击页面右上角更多按钮，选择课程表
2. 点击"从图库导入课程表"按钮
3. 验证可进入图片选择入口

预期结果：
1. 可打开课程表页面
2. 图库导入入口可点击
3. 可正常进入图片选择入口

```

## 步骤

   - ⚙️ pm clear com.zui.calendar
   - 👁 检查点：activity=.AllInOneActivity
1. **act tap** 定位 `rid=button1(同意)` —— 首次权限说明框同意
   - 🪟 弹窗：「日历正在尝试读取日历」
2. **act tap** 定位 `rid=timetable_guide_button` —— 关课程表上新引导
3. **act tap** 定位 `rid=iv_more` —— 更多
4. **act tap** 定位 `text=课程表` —— 进课程表
   - 👁 检查点：activity=.timetable.widget.TimetableGuideActivity
   - 👁 检查点：activity=.timetable.widget.TimetableGuideActivity（AI 当时在此页连续重复核对）
5. **act tap** 定位 `rid=timetable_guide_button` —— 关引导（第2次尝试）
6. **act tap** 定位 `rid=iv_more` —— 更多
   - 👁 检查点：activity=.AllInOneActivity
   - 👁 检查点：activity=.AllInOneActivity（AI 当时在此页连续重复核对）
7. **act tap** 定位 `rid=item_title(课程表)` —— 进课程表
   - 👁 检查点：activity=.timetable.display.TimetableActivity
8. **act tap** 定位 `rid=btnImportFromPhoto` —— 步骤1: 拍照导入课程表（拒绝分支）
   - 🪟 弹窗：「请确保图片清晰、完整」
   - 👁 检查点：activity=.timetable.display.TimetableActivity（AI 当时在此页连续重复核对）
9. **act tap** 定位 `rid=button1(知道了)` —— 关提示框触发权限弹窗
   - 🪟 弹窗：「需要权限」
10. **act tap** 定位 `rid=button2(取消)` —— 关掉需要权限提示框
   - ⚙️ pm revoke com.zui.calendar android.permission.CAMERA → not granted
11. **act tap** 定位 `rid=btnImportFromPhoto` —— 步骤1: 拍照导入（允许分支，默认同意）
   - 🪟 弹窗：「请确保图片清晰、完整」
12. **act tap** 定位 `rid=button1(知道了)` —— 关提示框触发权限
   - 👁 检查点：activity=.CaptureActivity
   - 👁 检查点：activity=.timetable.display.TimetableActivity
13. **act tap** 定位 `rid=btnImportFromGallery` —— 步骤2: 从图库导入（拒绝分支）
   - 🪟 弹窗：「请确保图片清晰、完整」
14. **act tap** 定位 `rid=button1(知道了)` —— 关提示框触发权限
   - 🪟 弹窗：「需要权限」
   - 👁 检查点：activity=.timetable.display.TimetableActivity（AI 当时在此页连续重复核对）
15. **act tap** 定位 `rid=button2(取消)` —— 关掉需要权限框
   - ⚙️ pm revoke com.zui.calendar android.permission.READ_MEDIA_IMAGES → not granted
16. **act tap** 定位 `rid=btnImportFromGallery` —— 步骤2: 从图库导入（允许分支）
   - 🪟 弹窗：「请确保图片清晰、完整」
17. **act tap** 定位 `rid=button1(知道了)` —— 关提示框触发权限
   - 👁 检查点：activity=com.android.providers.media.photopicker.PhotoPickerGetContentActivity

## 结论

联想日历_169 复跑通过（4/4 分支）。前提:pm clear 后相机/照片权限均未授予、无课程表。步骤1拍照导入: 拒绝→提示需要相机权限(CAMERA granted=false); 允许→相机打开(com.zui.camera/.CaptureActivity)。步骤2图库导入: 拒绝→提示需要存储权限(READ_MEDIA_IMAGES granted=false); 允许→弹窗含选择照片/全部允许,自动点全部允许→照片选择界面(PhotoPickerGetContentActivity)。本次按新约定④为每个预期结果补了显式 observe 检查点(带判据字段 activity+文本+dumpsys 值),权限弹窗全部由 perm-intent 自动响应(--perm-action 声明/默认同意)。证据 storage/evidence/w169/

## 回放说明

- 按步骤逐条执行，**每步核对返回**；任一步失配（元素找不到/状态不符）
  → 回退完整 observe→决策流程，跑通后重新 export 覆盖本卡。
- 带 🎯 的步骤会声明权限意图，照做即可；双分支场景注意两轮之间的 revoke。
- 带 🪟 的步骤说明当时是弹窗操作，`rid=button1` 之类**不能盲目复用**，
  要按弹窗标题确认当前是哪一层弹窗。
- 坐标为当次实测，⚠️ 标记的步骤回放前必须先 observe 校准。

> 提炼自会话 #129；回放失配 → 回退全流程并更新本卡。