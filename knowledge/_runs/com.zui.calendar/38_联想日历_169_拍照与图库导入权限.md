# 链路：联想日历_169_拍照与图库导入权限

- 来源会话: #38（PASS，2026-09-22T10:50:49.713）
- 包名: com.zui.calendar
- 设备: HA2J36T6
- 用例口述:

```
联想日历_169

前提：
1. 日历未授予相机/图库权限
2. 设备无课程表。（清理APP数据）

操作步骤：
1. 日历点击页面右上角更多按钮，选择课程表，点击"拍照导入课程表"按钮
2. 返回课程表设置页，点击"从图库导入课程表"按钮

预期结果：
1. 弹出权限弹窗，选择允许后打开相机即可，选择拒绝后提示需要授予相机权限
2. 弹出权限弹窗，可选择部分照片或全部允许，选择后可选择照片选择界面即可，选择拒绝后提示需要授予相册权限

```

## 步骤

   - ⚙️ pm clear com.zui.calendar
1. **act tap** 定位 `rid=button1` ｜ 🎯**声明**权限意图 `grant` —— 首次权限说明框点同意
2. **act tap** 定位 `rid=timetable_guide_button` —— 关课程表引导
   - 权限意图（延续）：grant
3. **act tap** 定位 `rid=iv_more` —— 步骤1: 更多
   - 权限意图（延续）：grant
4. **act tap** 定位 `text=课程表` —— 步骤1: 课程表
   - 权限意图（延续）：grant
5. **act tap** 定位 `rid=btnImportFromPhoto` ｜ 🎯**声明**权限意图 `deny` —— 步骤1: 拍照导入（拒绝分支）
   - 🪟 弹窗：「请确保图片清晰、完整」
6. **act tap** 定位 `rid=button1` —— 关提示框触发权限
   - 🪟 弹窗：「需要权限」
   - 权限意图（延续）：deny
   - 👁 检查点：activity=.timetable.display.TimetableActivity
7. **act tap** 定位 `rid=button2(取消)` —— 关掉需要权限框
   - 权限意图（延续）：deny
   - ⚙️ pm revoke com.zui.calendar android.permission.CAMERA → not granted
8. **act tap** 定位 `rid=btnImportFromPhoto` ｜ 🎯**声明**权限意图 `grant` —— 步骤1: 拍照导入（允许分支）
   - 🪟 弹窗：「请确保图片清晰、完整」
9. **act tap** 定位 `rid=button1` —— 关提示框触发权限
   - 权限意图（延续）：grant
10. **act tap** 定位 `rid=btnImportFromGallery` ｜ 🎯**声明**权限意图 `deny` —— 步骤2: 图库导入（拒绝分支）
   - 🪟 弹窗：「请确保图片清晰、完整」
11. **act tap** 定位 `rid=button1` —— 关提示框触发权限
   - 🪟 弹窗：「需要权限」
   - 权限意图（延续）：deny
   - 👁 检查点：activity=.timetable.display.TimetableActivity
12. **act tap** 定位 `rid=button2(取消)` —— 关掉需要权限框
   - 权限意图（延续）：deny
   - ⚙️ pm revoke com.zui.calendar android.permission.READ_MEDIA_IMAGES → not granted
13. **act tap** 定位 `rid=btnImportFromGallery` ｜ 🎯**声明**权限意图 `grant` —— 步骤2: 图库导入（允许分支）
   - 🪟 弹窗：「请确保图片清晰、完整」
14. **act tap** 定位 `rid=button1` —— 关提示框触发权限
   - 权限意图（延续）：grant

## 结论

联想日历_169 复跑结果：全部通过（4/4 分支）

前提满足：pm clear 后相机/照片权限均未授予，无课程表。
步骤1 拍照导入：拒绝→提示需要相机权限；允许→相机打开
步骤2 图库导入：拒绝→提示需要存储权限；允许→照片选择界面打开

执行方式：权限弹窗全部由 perm-intent 声明意图自动响应（先拒后允，每轮前 revoke 重置），
全程未使用 read_image（改用 --full 读节点文字确认提示语）。
证据：storage/evidence/r2/


## 回放说明

- 按步骤逐条执行，**每步核对返回**；任一步失配（元素找不到/状态不符）
  → 回退完整 observe→决策流程，跑通后重新 export 覆盖本卡。
- 带 🎯 的步骤会声明权限意图，照做即可；双分支场景注意两轮之间的 revoke。
- 带 🪟 的步骤说明当时是弹窗操作，`rid=button1` 之类**不能盲目复用**，
  要按弹窗标题确认当前是哪一层弹窗。
- 坐标为当次实测，⚠️ 标记的步骤回放前必须先 observe 校准。

> 提炼自会话 #38；回放失配 → 回退全流程并更新本卡。