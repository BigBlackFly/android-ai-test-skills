# 链路：联想日历_169_拍照与图库导入权限

- 来源会话: #135（PASS，2026-09-22T15:09:02.645）
- 包名: com.zui.calendar
- 设备: HA2J36T6
- 用例口述:

```
联想日历_169

前提：
1. 日历未授予相机/图库权限
2. 设备无课程表。（清理APP数据）

操作步骤：
1. 日历点击页面右上角更多按钮，选择课程表，点击"拍照导入课程表"按钮
2. 返回课程表设置页，点击"从图库导入课程表"按钮

预期结果：
1. 弹出权限弹窗，选择允许后打开相机即可，选择拒绝后提示需要授予相机权限
2. 弹出权限弹窗，可选择部分照片或全部允许，选择后可选择照片选择界面即可，选择拒绝后提示需要授予相册权限

```

## 步骤

   - ⚙️ pm clear com.zui.calendar
   - 👁 检查点：activity=.AllInOneActivity
1. **act tap** 定位 `rid=button1(同意)` —— 首次权限说明框同意
   - 🪟 弹窗：「日历正在尝试读取日历」
   - 🔐 权限弹窗自动响应：允许
   - 👁 检查点：activity=.permission.ui.GrantPermissionsActivity
2. **act tap** 定位 `rid=timetable_guide_button` —— 关引导
   - 🪟 弹窗：「日历正在尝试读取联系人」
   - 🔐 权限弹窗自动响应：允许
   - 👁 activity=.permission.ui.GrantPermissionsActivity（与前一步相同，连续重复核对）
3. **act tap** 定位 `rid=iv_more` —— 更多
   - 🪟 弹窗：「日历正在尝试获取设备信息」
   - 🔐 权限弹窗自动响应：允许
   - 👁 检查点：activity=.timetable.widget.TimetableGuideActivity
   - ⚙️ pm revoke com.zui.calendar android.permission.CAMERA → not granted
   - 👁 检查点：activity=.AllInOneActivity
4. **act tap** 定位 `rid=iv_more` —— 更多
   - 👁 activity=.AllInOneActivity（与前一步相同，连续重复核对）
5. **act tap** 定位 `rid=item_title(课程表)` —— 进课程表
   - 👁 检查点：activity=.timetable.display.TimetableActivity
6. **act tap** 定位 `rid=btnImportFromPhoto` ｜ 🎯**声明**权限意图 `deny`（camera） —— 步骤1: 拍照导入（拒绝分支）
   - 🪟 弹窗：「请确保图片清晰、完整」
   - 👁 activity=.timetable.display.TimetableActivity（与前一步相同，连续重复核对）
7. **act tap** 定位 `rid=button1(知道了)` —— 关提示框触发权限
   - 🪟 弹窗：「需要权限」
   - 🔐 权限弹窗自动响应：拒绝
   - 权限意图（延续）：deny
8. **act tap** 定位 `rid=button2(取消)` —— 关需要权限框
   - 权限意图（延续）：deny
   - ⚙️ pm revoke com.zui.calendar android.permission.CAMERA → not granted
9. **act tap** 定位 `rid=btnImportFromPhoto` —— 步骤1: 拍照导入（允许分支）
   - 🪟 弹窗：「请确保图片清晰、完整」
   - 权限意图（延续）：deny
10. **act tap** 定位 `rid=button1(知道了)` —— 关提示框触发权限（默认同意）
   - 🔐 权限弹窗自动响应：仅在使用时允许
   - 权限意图（延续）：deny
   - 👁 检查点：activity=.CaptureActivity
11. **act tap** 定位 `rid=btnImportFromGallery` ｜ 🎯**声明**权限意图 `deny`（media_images） —— 步骤2: 图库导入（拒绝分支）
   - 🪟 弹窗：「请确保图片清晰、完整」
   - 👁 检查点：activity=.timetable.display.TimetableActivity
12. **act tap** 定位 `rid=button1(知道了)` —— 关提示框触发权限
   - 🪟 弹窗：「需要权限」
   - 🔐 权限弹窗自动响应：拒绝
   - 权限意图（延续）：deny
   - 👁 activity=.timetable.display.TimetableActivity（与前一步相同，连续重复核对）
13. **act tap** 定位 `rid=button2(取消)` —— 关需要权限框
   - 权限意图（延续）：deny
   - ⚙️ pm revoke com.zui.calendar android.permission.READ_MEDIA_IMAGES → not granted
14. **act tap** 定位 `rid=btnImportFromGallery` —— 步骤2: 图库导入（允许分支）
   - 🪟 弹窗：「请确保图片清晰、完整」
   - 权限意图（延续）：deny
15. **act tap** 定位 `rid=button1(知道了)` —— 关提示框触发权限（默认同意）
   - 🔐 权限弹窗自动响应：全部允许
   - 权限意图（延续）：deny
   - 👁 检查点：activity=com.android.providers.media.photopicker.PhotoPickerGetContentActivity

## 结论

联想日历_169 复跑通过（4/4 分支）。本次按修正后的规矩执行：验证类步骤一律用 act --full（动作+全量节点一次拿到，其 activity 与文本即检查点），除会话第一步外未单独 observe。四个分支：步骤1拒绝→需要相机权限提示；步骤1允许→相机打开；步骤2拒绝→需要存储权限提示；步骤2允许→照片选择界面。证据 storage/evidence/z169/

## 回放说明

- 按步骤逐条执行，**每步核对返回**；任一步失配（元素找不到/状态不符）
  → 回退完整 observe→决策流程，跑通后重新 export 覆盖本卡。
- 带 🎯 的步骤会声明权限意图，照做即可；双分支场景注意两轮之间的 revoke。
- 带 🪟 的步骤说明当时是弹窗操作，`rid=button1` 之类**不能盲目复用**，
  要按弹窗标题确认当前是哪一层弹窗。
- 坐标为当次实测，⚠️ 标记的步骤回放前必须先 observe 校准。

> 提炼自会话 #135；回放失配 → 回退全流程并更新本卡。